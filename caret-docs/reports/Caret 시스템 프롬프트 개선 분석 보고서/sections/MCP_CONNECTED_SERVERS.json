{
	"description": "Information about connected MCP servers.",
	"tools": ["use_mcp_tool", "access_mcp_resource"],
	"content": "When a server is connected, you can use the server's tools via the `use_mcp_tool` tool, and access the server's resources via the `access_mcp_resource` tool.",
	"mcp_connected_servers_list": "{{mcp_connected_servers_list}}"
}
